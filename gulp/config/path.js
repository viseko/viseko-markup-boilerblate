/* global app */
const srcFolder = "./src";
const publicFolder = "./public";

const pathes = {
  srcFolder,
  publicFolder,

  src: {
    img: `${srcFolder}/img/`,
  },
  public: {
    img: `${publicFolder}/img/`,
  },
};

export default pathes;

extends common/layout.pug

block body
  h1 Viseko Markup Boilerplate
  p Здесь однажды будет вёрстка крутого сайта, а пока вот так
  hr
  
  h2 SVG-icons
  +svgIcon("fold", "Vector", 40, 40)
  +svgIcon("fold", "cut", 40, 40)
  +svgIcon("fold", "copy", 40, 40)
  +svgIcon("fold", "link", 40, 40)
  br
  a(href="files/somefile.docx") 
    | Скачать штуку
